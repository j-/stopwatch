/** For example shows 21:15 as "9:15". */
export const TIME_FORMAT = 'h:mm';

/** How frequently, in milliseconds, the clock is updated. */
export const TIME_INTERVAL = 1000;

/** How frequently, in milliseconds, the stopwatch is updated. */
export const STOPWATCH_INTERVAL = 150;

/** When, in seconds, to stop incrementing the stopwatch counter. */
export const MAX_VALUE_SECONDS = 36000 - 1;
